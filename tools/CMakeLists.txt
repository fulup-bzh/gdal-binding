
PROJECT_TARGET_ADD(gdal-client)
    file(GLOB CSOURCES [a-z]*.cpp)
    add_executable(${TARGET_NAME} ${CSOURCES})

    SET_TARGET_PROPERTIES(${TARGET_NAME} PROPERTIES
	LABELS "EXECUTABLE"
	OUTPUT_NAME ${TARGET_NAME}
    )